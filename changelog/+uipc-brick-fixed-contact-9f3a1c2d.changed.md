Keep `SolverUIPC`'s built-in contact pairs scene-adaptive while configuring
the `uipc_brick_stacking` example with the measured corridor minimum
(`9.538500988573645e9` Pa). Expose the measured bounds as
`SolverUIPC.ADAPTIVE_KAPPA_MIN` and `SolverUIPC.ADAPTIVE_KAPPA_MAX`.
