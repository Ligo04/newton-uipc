Rename `ControllerStablePD` to `DriveStablePD` and move it from `newton/_src/actuators/controllers/` to `newton/_src/actuators/drives/drive_stable_pd.py`, following the upstream `Controller*` to `Drive*` rename. `DriveStablePD` now derives from `DriveBase`, `NewtonStablePDControlAPI` registers under `ComponentKind.DRIVE`, and the UIPC examples, benchmark scripts, and tests use `drive_class`, `drive_state`, and `Actuator.drive` instead of their `controller*` predecessors.
